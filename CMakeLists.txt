cmake_minimum_required(VERSION 3.24)

# Milestone 1 is Python-only. C++ and CUDA targets are introduced only once their
# Python reference contracts and tests exist.
project(hpti LANGUAGES NONE)

message(STATUS "hpti Milestone 1: no C++/CUDA targets are defined yet")
